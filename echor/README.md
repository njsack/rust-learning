# echor
```
echor 0.1.0
Simple echo program

USAGE:
    echor [OPTIONS] <TEXT>...

ARGS:
    <TEXT>...    Input to echo

OPTIONS:
    -h, --help          Print help information
    -n, --no-newline    Omit newline on print
    -V, --version       Print version information
```